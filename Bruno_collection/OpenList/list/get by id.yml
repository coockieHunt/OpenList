info:
  name: get by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/list/{{idList}}"
  auth: inherit

runtime:
  variables:
    - name: idList
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
