info:
  name: dell list
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/list/{{idList}}"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: idList
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
