info:
  name: validate item
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/item/{{idList}}/{{idItem}}"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: idList
      value: "1"
    - name: idItem
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
