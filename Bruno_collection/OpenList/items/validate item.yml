info:
  name: validate item
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/item/1"
  body:
    type: json
    data: |-
      {
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
