info:
  name: validate item
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/item/1
  body:
    type: json
    data: |-
      {
          "name": "{{$randomWord}}",
          "quantity": 2,
          "validated": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
