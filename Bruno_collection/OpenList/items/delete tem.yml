info:
  name: delete tem
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:8080/item/1
  body:
    type: json
    data: |-
      {
        id: 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
